const JSON_HEADERS={"content-type":"application/json"};
export class ApiError extends Error{constructor(message,code="REQUEST_FAILED",status=0,detail=null){super(message);this.name="ApiError";this.code=code;this.status=status;this.detail=detail}}
export async function api(path,options={}){const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),options.timeout||10000);try{const headers=new Headers(options.headers||{});if(options.body&&!(options.body instanceof FormData)&&!headers.has("content-type"))headers.set("content-type","application/json");const response=await fetch(path,{...options,headers,signal:options.signal||controller.signal,credentials:"same-origin"});let payload=null;const type=response.headers.get("content-type")||"";if(type.includes("json"))payload=await response.json();else if(response.status!==204)payload=await response.text();if(!response.ok){const error=payload?.error||{};throw new ApiError(error.message||`Request failed (${response.status})`,error.code||"REQUEST_FAILED",response.status,error)}return payload}catch(error){if(error.name==="AbortError")throw new ApiError("The station did not respond in time.","TIMEOUT");throw error}finally{clearTimeout(timeout)}}
export const get=(path)=>api(path);export const post=(path,body)=>api(path,{method:"POST",headers:JSON_HEADERS,body:JSON.stringify(body)});export const patch=(path,body)=>api(path,{method:"PATCH",headers:JSON_HEADERS,body:JSON.stringify(body)});export const del=(path,body)=>api(path,{method:"DELETE",headers:JSON_HEADERS,body:body?JSON.stringify(body):undefined});
