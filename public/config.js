/* SIDEBAND unified-Worker configuration.
 *
 * The interface and API are served together beneath /radio, so apiBaseUrl
 * normally remains blank. The browser automatically uses the directory that
 * contains the current page as its API and media base.
 * Never put tokens, passwords, or private storage keys in this file.
 */
window.SIDEBAND_CONFIG = Object.freeze({
  apiBaseUrl: "",
});
