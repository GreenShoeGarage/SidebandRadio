export class HttpError extends Error{constructor(status,code,message,recovery=null,fields=null,details=null){super(message);this.status=status;this.code=code;this.recovery=recovery;this.fields=fields;this.details=details}}
export function correlationId(request){return request.headers.get("cf-ray")||crypto.randomUUID()}
export function json(data,status=200,headers={}){return new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff",...headers}})}
export function errorResponse(error,id){const safe=error instanceof HttpError?error:new HttpError(500,"INTERNAL_ERROR","SIDEBAND could not complete the request.","Retry once. If the problem continues, export diagnostics and inspect Worker logs.");return json({error:{code:safe.code,message:safe.message,fieldErrors:safe.fields||undefined,details:safe.details||undefined,correlationId:id,recovery:safe.recovery||undefined}},safe.status)}
export function methodNotAllowed(allowed){throw new HttpError(405,"METHOD_NOT_ALLOWED","That request method is not supported.",`Use one of: ${allowed.join(", ")}.`)}
